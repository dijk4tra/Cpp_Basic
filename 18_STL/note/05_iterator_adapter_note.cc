#include <iostream>
#include <vector>
#include <iterator>
#include <algorithm>
#include <string>
#include <fstream>

using namespace std;

/**
 * 迭代器适配器
 * 1. back_inserter_iterator  : 尾部插入的迭代器
 * 2. front_inserter_iterator : 头部插入的迭代器
 * 3. insert_iterator : 中间位置插入的迭代器
 * 以上三种都是类型
 * 
 * back_inserter()   : 创建一个back_inserter_iterator对象
 * front_inserter()  : 创建一个front_inserter_iterator对象
 * insert_iterator() : 创建一个insert_iterator对象
 * 以上三个是函数，用于创建对应的迭代器对象
 * 
 */



void test1()
{

}


int main(int argc, char *argv[])
{
    test1();

    return 0;
}