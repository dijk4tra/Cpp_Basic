/**
 * 使用`std::vector`存储多个学生信息（学生类为前面定义的`Student`类），实现以下功能：
 * 要求：
 * 1. 创建3个学生对象，存入`vector`；
 * 2. 使用增强for循环遍历并输出所有学生信息；
 * 3. 使用迭代器遍历并输出所有学生信息；
 * 4. 向`vector`末尾添加一个新学生，并再次输出。
 */

#include <iostream>
#include <string>
#include <vector>



