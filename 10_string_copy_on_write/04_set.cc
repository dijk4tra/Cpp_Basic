#include <iostream>

using std::cout;
using std::endl;

/*
 * 关联式容器：set、map
 *
 * set：底层结构通常使用红黑树实现
 *
 * 特点：
 * 1. 存储单列数据，即一个一个独立的数据
 *    例如：int 类型的 1、2、3、4、5
 *          string 类型的 "abc"、"hello"
 *          或者自定义类型 Point 对象
 * 2. set 中存储的数据是有序的，默认按照升序排列
 * 3. set 中存储的数据不能重复
 */
void test1()
{

}

int main(int argc, char * argv[])
{
    test1();
    return 0;
}